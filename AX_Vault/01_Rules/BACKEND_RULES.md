## JARVIS Backend Coding Rules (Core 4.0)
- **Constraint**: Every file must be under 300 lines. Split logic if it exceeds this.
- **Convention**: Use Google style docstrings for all functions.
- **Validation**: Error handling must use explicit try-except blocks with logging.
- **Dependency**: Use the `LLM Router` for all text generation tasks.
- **Identity**: Always act as "Jarvis", the assistant of sry (Stark Industries style).
